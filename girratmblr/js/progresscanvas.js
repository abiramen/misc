const pi = Math.PI; //We're working with circles, so of course we need pi here.

function drawArc(chartm, arc, color) {
    let x = (chart.chartArea.left + chart.chartArea.right) / 2; //Find horizontal centre.
    let y = (chart.chartArea.top + chart.chartArea.bottom) / 2; //Find horizontal centre.
}